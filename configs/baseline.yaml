model:
  encoder_channels: [64, 128, 256]
  latent_dim: 512
  num_eq_bands: 5
  
training:
  batch_size: 16
  learning_rate: 0.0001
  epochs: 100
  
data:
  data_dir: "C:/Users/danie/Documents/!ELEC0030 Project/neural-perceptual-master training/processed/train"
  segment_length: 5.0
  sample_rate: 44100
  augment: true
  num_workers: 4
  normalize_lufs: true
  target_lufs: -14.0
  
loss:
  spectral_weight: 1.0
  perceptual_weight: 0.1
  loudness_weight: 0.01